/**
 * 自定义store getters
 * @module store/getters
 *
 * @example 定义getter方式
 *
 * getterExample(state, getters) {
 *   // do something...
 *   return state.list.length // 必须要有返回值
 * }
 *
 */
export default {}
